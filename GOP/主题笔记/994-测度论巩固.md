

- $\mu_Z$ 是$（ \qquad）$ 测度，他对单元素集合的测度等于$（ \qquad）$
- $\mu$ 是一个测度的三要素：(1) $（ \qquad）$ (2) $（ \qquad）$ (3)$（ \qquad）$
- σ-代数需要满足三条公理：(1)   $（ \qquad）$(2)   $（ \qquad）$ (3)$（ \qquad）$
- $A \subseteq B$，有 $\mu(A) \leq \mu(B)$，这是测度的$（ \qquad）$性质
- $\lambda$ 是$（ \qquad）$ 测度，他对单元素集合的测度等于$（ \qquad）$, 对于（a,b) 的测度值为$（ \qquad）$
-  $\Omega$ = $\{1,2,3,4,5,6\}$，投掷三次筛子结果之和的样本空间$（ \qquad）$，2次投掷筛子的结果的结果空间$（ \qquad）$
- 对于两两不相交的集合 $A_i$ ，$\mu(\bigcup A_{i})=\sum\mu(A_{i})$ 是测度的$（ \qquad）$性质
- 在样本空间 $\Omega$ 内，集合A的补集合可以写为 $A^c=$ $（ \qquad）$
- 两个集合测度相同，两个集合是否相同$（ \qquad）$？
- 函数 如果是 $f:\Omega \to \mathbb{R}$ ，这是$（ \qquad）$ 可测，如果是 $f:\Omega \to \Omega$ ，这是$（ \qquad）$可测，随机变量是$（ \qquad）$ 可测
- 样本空间 $\Omega$ 的最大σ-代数是$（ \qquad）$, 最小 σ-代数是$（ \qquad）$
- 两个集合 A，B，如果 $A \subseteq B$，根据测度的单调性，对于测度 $\mu$ 有$（ \qquad）$
- 测度 $\mu$ 的零测集 （Nullmenge）的定义为：$（ \qquad）$
- L-S测度下，对 **右连续** **单调增** 函数 $G$， $\lambda_G((a,b]) =$$（ \qquad）$
- 可测函数 $f$ 的定义：$f^{-1}(B)=$
- Dirac 测度 $\delta_x$：若 $x \in A$，则 $\delta_x(A) =$$（ \qquad）$，否则 $\delta_x(A) =$$（ \qquad）$
- $\mathcal{F}_1$-$\mathcal{F}_2$ 可测：验证方法是：
- **勒贝格积分**
	- $\int_{\Omega} fd\mu =\Sigma_{i=1}^n a_i \cdot \mu(f^{-1}(\{a_i\}))$  ：**函数 $f$ 在 样本空间 $\Omega$ 上关于测度 $\mu$ 的积分**
		- **左侧**：
			- 这个是在$（ \qquad）$空间的勒贝格积分
			- $f$ 这个是定义在 $\Omega$ 上的$（ \qquad）$
			- $d\mu$ 是关于测度 $\mu$ 的积分，指定积分时候使用的“**权重**”，如果使用 $\mu_Z$ 测度，即每个点的测度为$（ \qquad）$，也可以使用 $\mu(A) = \Sigma_{\omega \in A} \omega$ 等自定义权重测度
		- **右侧**：
			- $a_i$ ：是 $\Omega$ 样本空间里面元素 $\omega$ 经过可测函数 $f$ 映射后的一个 **实数**
			- $\mu(f^{-1}(\{a_i\})$ ：通过可测函数 $f$ 的逆映射 $f^{-1}$ 将 $a_i$ 映射回 $（ \qquad）$后，再通过测度 $\mu$ 计算得到一个 **实数**
	 - **计数测度形式**：$\int_{\Omega} fd\mu_Z = \Sigma_{\omega \in \Omega} f(\omega) \cdot \mu(\{\omega\})$
	 - **区间勒贝格测度形式**：$\int_{[a,b]}fd\lambda = \int_a^b f(x)dx$
- **前推测度**：像空间上的测度 $\mu_f(A)$ 是 原像空间测度 $\mu(A)$ 的前推测度条件是：$\mu_f(A) = \mu$$（ \qquad）$= $\mu(\{\omega \in \Omega: f(\omega) \in A\})$   
- **Radon-Nikodym**：如果测度 $\nu$ 可以写为测度 $\mu$ 的积分形式，即 $\nu(A) = \int_A fd\mu$，**判定条件是**：$（ \qquad）$ $\Rightarrow$ $（ \qquad）$




