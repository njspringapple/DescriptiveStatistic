# 期望、方差、不等式与正态分布

练习题数：19

相关考试真题数：3

合计题目数：22

## 公式速查

### 考试可用版

- **期望**：$E(X)=\sum_xxP(X=x)$ 或 $E(X)=\int xf_X(x)dx$
- **线性性**：$E(aX+bY)=aE(X)+bE(Y)$
- **函数期望**：$E(g(X))=\sum_xg(x)P(X=x)$ 或 $\int g(x)f_X(x)dx$
- **方差**：$Var(X)=E(X^2)-E(X)^2$
- **样本方差**：$S^2=\frac1{n-1}\sum_i(x_i-\bar x)^2$
- **方差缩放**：$Var(aX+b)=a^2Var(X)$
- **和的方差**：$Var(X+Y)=Var(X)+Var(Y)+2Cov(X,Y)$
- **协方差**：$Cov(X,Y)=E(XY)-E(X)E(Y)$
- **相关系数**：$\rho=\frac{Cov(X,Y)}{\sigma_X\sigma_Y}$
- **Markov**：$P(|X|\ge a)\le\frac{E|X|}{a}$
- **Chebyshev**：$P(|X-\mu|\ge c)\le\frac{Var(X)}{c^2}$
- **正态标准化**：$Z=\frac{X-\mu}{\sigma}\sim N(0,1)$
- **配对 t**：$t=\frac{\bar D-\mu_0}{s_D/\sqrt n}$
- **Welch t**：$t=\frac{\bar X-\bar Y-\Delta_0}{\sqrt{s_X^2/n_X+s_Y^2/n_Y}}$
- **Wilcoxon**：$W^+=\sum_{D_i>0}Rang(|D_i|)$

### 不会时怎么下手

- **题目问期望**：先写定义，再代入；连续型别忘了多乘一个 $x$。
- **题目问方差**：通常走 $Var(X)=E(X^2)-E(X)^2$，先算 $E(X)$ 和 $E(X^2)$。
- **题目有线性变换**：期望乘 $a$，方差乘 $a^2$，加常数不影响方差。
- **题目问协方差**：先算 $E(XY)$、$E(X)$、$E(Y)$，再用移项公式。
- **题目问不等式上界**：只知道期望用 Markov；知道均值和方差用 Chebyshev。
- **题目是正态概率**：先标准化成 $Z$，再查标准正态。
- **题目是检验**：先判断配对/独立，再写假设、统计量、临界值或 p 值、结论。

---

## 习题与讲解

### Aufgabe 1 - 用 Jensen 不等式推导期望或均值不等式。

#### 题目

Sei $X$ eine reellwertige Zufallsvariable mit Dichte $f_X$. Für eine weitere Dichtefunktion $f_Y(y)$ ist die Kullback-Leibler-Divergenz der beiden Dichten definiert als

译：设 $X$ 是实值随机变量，密度为 $f_X$。对另一个密度函数 $f_Y(y)$，两个密度之间的 Kullback-Leibler 散度定义为：

$$
D(f_X,f_Y)
:=
E_X\left[
\log\left(
\frac{f_X(X)}{f_Y(X)}
\right)
\right].
$$

Der Erwartungswert wird bezüglich der Verteilung von $X$ mit Dichte $f_X$ gebildet.

译：这里的期望是关于具有密度 $f_X$ 的 $X$ 的分布来计算的。

###### (a)

Zeigen Sie, dass

译：证明：

$$
Z:=\frac{f_Y(X)}{f_X(X)}
$$

eine Zufallsvariable ist.

译：是一个随机变量。

###### (b)

Zeigen Sie:

译：证明：

$$
D(f_X,f_Y)\geq 0.
$$

Hinweis: Verwenden Sie die Jensen'sche Ungleichung und $Z$ aus Aufgabe (a).

译：提示：使用 Jensen 不等式以及 (a) 中的 $Z$。

#### 解答

##### 中文解题思路

看到分布函数或密度，先检查对象类型：离散型看跳跃点概率，连续型看密度积分。分布函数要满足单调、右连续、极限从 $0$ 到 $1$；密度要非负且总积分为 $1$。

Jensen 题最重要的是先判断函数凸凹。凸函数给 $\varphi(\mathbb E X)\le \mathbb E\varphi(X)$；凹函数不等号反向。写解答时要明确说明使用的是凸性还是凹性。

测度/可测性题要回到定义：可测性通常看原像是否属于 $\sigma$-Algebra；Lebesgue 测度算长度或面积；计数测度算元素个数；Dirac 测度只看集合是否包含支撑点。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

Jensen 题先判断函数凸还是凹。凸函数给 $\varphi(\mathbb E X)\le \mathbb E\varphi(X)$，凹函数方向相反；考试中要把凸性判断写出来。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a) Zeigen Sie, dass

Da $X$ eine Zufallsvariable ist und $f_X$ sowie $f_Y$ messbare Funktionen sind, sind auch die Verkettungen

$$
f_X(X)
\qquad
\text{und}
\qquad
f_Y(X)
$$

Zufallsvariablen.

Auf der Menge, auf der $f_X(X)>0$ gilt, ist daher auch der Quotient

$$
Z=\frac{f_Y(X)}{f_X(X)}
$$

messbar und somit eine Zufallsvariable.

Auf eventuellen Nullmengen, auf denen der Quotient nicht definiert ist, kann $Z$ beliebig definiert werden, ohne die Aussage bezüglich der Verteilung von $X$ zu ändern.

###### (b) Zeigen Sie:

Aus der Definition von $Z$ folgt:

$$
\log\left(\frac{f_X(X)}{f_Y(X)}\right)
=
\log\left(\frac{1}{Z}\right)
=
-\log Z.
$$

Also:

$$
D(f_X,f_Y)=E_X[-\log Z].
$$

Die Funktion

$$
h(z)=-\log z
$$

ist konvex auf $(0,\infty)$.

Nach Jensen gilt:

$$
E_X[-\log Z]\geq -\log(E_X[Z]).
$$

Nun berechnen wir $E_X[Z]$:

$$
E_X[Z]
=
\int \frac{f_Y(x)}{f_X(x)}f_X(x)\,dx
=
\int f_Y(x)\,dx
=
1.
$$

Damit:

$$
D(f_X,f_Y)
=
E_X[-\log Z]
\geq
-\log(1)
=
0.
$$

Also ist:

$$
D(f_X,f_Y)\geq 0.
$$

---

### Aufgabe 2 - 计算期望、方差、协方差或相关系数。

#### 题目

Würfelspiel: Auf einem fairen Würfel wird die $1$ als $1$, die $2$ und $3$ als $2$, und die restlichen Augenzahlen als $3$ zugeordnet. Geben Sie Maßräume und Zufallsvariable an, begründen Sie die Wahrscheinlichkeitsräume und berechnen Sie den Erwartungswert.

译：骰子游戏：掷一个公平骰子，点数 $1$ 记为 $1$，点数 $2$ 和 $3$ 记为 $2$，其余点数记为 $3$。给出测度空间和随机变量，说明概率空间为何成立，并计算期望。

#### 解答

##### 中文解题思路

Laplace 模型的第一步是数清样本空间大小和有利结果个数。最后概率写成 $|A|/|\Omega|$；如果事件之间有重叠，要用容斥原理而不是直接相加。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

Ausgangsraum:

$$
\Omega=\{1,2,3,4,5,6\},
\qquad
\mathcal F=\mathcal P(\Omega),
\qquad
\mathbb P(\{i\})=\frac16.
$$

Zielraum:

$$
S=\{1,2,3\},
\qquad
\mathcal S=\mathcal P(S).
$$

Die Zufallsvariable $X:\Omega\to S$ ist:

$$
X(\omega)=
\begin{cases}
1, & \omega=1,\\
2, & \omega\in\{2,3\},\\
3, & \omega\in\{4,5,6\}.
\end{cases}
$$

Die induzierte Verteilung ist:

$$
\mathbb P_X(\{1\})=\frac16,
\qquad
\mathbb P_X(\{2\})=\frac13,
\qquad
\mathbb P_X(\{3\})=\frac12.
$$

Beide Räume sind Wahrscheinlichkeitsräume, da die jeweiligen Gesamtwahrscheinlichkeiten $1$ sind.

Der Erwartungswert:

$$
\mathbb E[X]
=
1\cdot\frac16
+2\cdot\frac13
+3\cdot\frac12
=\frac73.
$$

---

### Aufgabe 3 - 练习期望、方差和概率不等式。

#### 题目

Sei $(X_n)_{n\in\mathbb N}$ eine Folge unabhängiger, diskreter Zufallsvariablen mit:

译：设 $(X_n)_{n\in\mathbb N}$ 是一列相互独立的离散随机变量，满足：

$$
\mathbb P(X_n=1)=\mathbb P(X_n=-1)=\frac12
\qquad
\text{für alle }n\in\mathbb N.
$$

Bestimmen Sie $\mathbb E(X_n)$ und $\operatorname{Var}(X_n)$ für ein festes $n\in\mathbb N$.

译：对固定的 $n\in\mathbb N$，求 $\mathbb E(X_n)$ 和 $\operatorname{Var}(X_n)$。

#### 解答

##### 中文解题思路

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

$$
\mathbb E(X_n)
=1\cdot\frac12+(-1)\cdot\frac12
=0.
$$

Außerdem:

$$
\mathbb E(X_n^2)
=1^2\cdot\frac12+(-1)^2\cdot\frac12
=1.
$$

Damit:

$$
\operatorname{Var}(X_n)
=\mathbb E(X_n^2)-\mathbb E(X_n)^2
=1.
$$

---

### Aufgabe 4 - 用 Markov 或 Chebyshev 不等式给概率上界。

#### 题目

Die Zufallsvariable $X$ sei die Augenzahl beim Wurf eines fairen achtseitigen Würfels:

译：设随机变量 $X$ 为掷一个公平八面骰得到的点数：

$$
\mathbb P(X=i)=\frac18,
\qquad i=1,\dots,8.
$$

###### (a)

Berechnen Sie $\mathbb E(X)$, $\operatorname{Var}(X)$ und $\mathbb P(X\geq 6)$.

译：计算 $\mathbb E(X)$、$\operatorname{Var}(X)$ 和 $\mathbb P(X\geq 6)$。

###### (b)

Bestimmen Sie eine obere Schranke für $\mathbb P(X\geq 6)$ mit der Markow-Ungleichung.

译：用 Markov 不等式给出 $\mathbb P(X\geq 6)$ 的上界。

###### (c)

Bestimmen Sie eine obere Schranke für $\mathbb P(X\geq 6)$ mit der Chebyshev-Ungleichung.

译：用 Chebyshev 不等式给出 $\mathbb P(X\geq 6)$ 的上界。

#### 解答

##### 中文解题思路

Laplace 模型的第一步是数清样本空间大小和有利结果个数。最后概率写成 $|A|/|\Omega|$；如果事件之间有重叠，要用容斥原理而不是直接相加。

不等式题先判断可用条件：Markov 用非负性控制 $P(X\ge a)$，Chebyshev 用均值和方差控制 $P(|X-\mu|\ge\varepsilon)$。算出的上界如果超过 $1$，要说明概率上界只能取平凡界 $1$。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

不等式题要先判断用哪个条件：Markov 需要非负随机变量并控制上尾；Chebyshev 需要均值和方差并控制偏离均值的概率。算出界以后还要检查是否超过 $1$，超过时只能说明界很粗。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a)

Für die diskrete Gleichverteilung auf $\{1,\dots,8\}$ gilt:

$$
\mathbb E(X)=\frac{1+8}{2}=\frac92.
$$

$$
\operatorname{Var}(X)=\frac{8^2-1}{12}=\frac{63}{12}=\frac{21}{4}.
$$

Außerdem:

$$
\mathbb P(X\geq 6)
=\mathbb P(X\in\{6,7,8\})
=\frac38.
$$

###### (b)

Da $X\geq 0$ gilt:

$$
\mathbb P(X\geq 6)
\leq
\frac{\mathbb E(X)}6
=
\frac{9/2}{6}
=\frac34.
$$

###### (c)

Da $\mathbb E(X)=\frac92$, folgt aus $X\geq 6$:

$$
X-\mathbb E(X)\geq \frac32.
$$

Also:

$$
\mathbb P(X\geq 6)
\leq
\mathbb P\left(\left|X-\frac92\right|\geq \frac32\right)
\leq
\frac{\operatorname{Var}(X)}{(3/2)^2}
=
\frac{21/4}{9/4}
=\frac73.
$$

Diese Chebyshev-Schranke ist größer als $1$ und daher nur trivial. Als Wahrscheinlichkeitsgrenze kann man sie zu $1$ verbessern.

---

### Aufgabe 5 - 用 Jensen 不等式推导期望或均值不等式。

#### 题目

Sei $X$ eine stetige Zufallsvariable mit Dichte:

译：设 $X$ 是具有如下密度的连续随机变量：

$$
f(x)=x+kx^2
\qquad
\text{für }0\leq x\leq 1,
$$

und $f(x)=0$ sonst. Dabei ist $k\in\mathbb R$.

译：其余情况下 $f(x)=0$。其中 $k\in\mathbb R$。

###### (a)

Bestimmen Sie $k$, sodass $f$ eine gültige Dichte ist.

译：求 $k$，使得 $f$ 是合法密度。

###### (b)

Bestimmen Sie $\mathbb E(X)$.

译：求 $\mathbb E(X)$。

###### (c)

Verwenden Sie die Jensen-Ungleichung, um eine Schranke für $\mathbb E(\exp(X))$ zu finden.

译：使用 Jensen 不等式，为 $\mathbb E(\exp(X))$ 找一个界。

#### 解答

##### 中文解题思路

看到分布函数或密度，先检查对象类型：离散型看跳跃点概率，连续型看密度积分。分布函数要满足单调、右连续、极限从 $0$ 到 $1$；密度要非负且总积分为 $1$。

Jensen 题最重要的是先判断函数凸凹。凸函数给 $\varphi(\mathbb E X)\le \mathbb E\varphi(X)$；凹函数不等号反向。写解答时要明确说明使用的是凸性还是凹性。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

Jensen 题先判断函数凸还是凹。凸函数给 $\varphi(\mathbb E X)\le \mathbb E\varphi(X)$，凹函数方向相反；考试中要把凸性判断写出来。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a) Bestimmen Sie $k$, sodass $f$ eine gültige Dichte ist.

Es muss gelten:

$$
\int_0^1 (x+kx^2)\,dx=1.
$$

Also:

$$
\frac12+\frac{k}{3}=1.
$$

Damit:

$$
k=\frac32.
$$

###### (b) Bestimmen Sie $\mathbb E(X)$.

$$
\mathbb E(X)
=\int_0^1 x\left(x+\frac32x^2\right)\,dx
=\int_0^1\left(x^2+\frac32x^3\right)\,dx.
$$

Also:

$$
\mathbb E(X)
=\frac13+\frac32\cdot\frac14
=\frac13+\frac38
=\frac{17}{24}.
$$

###### (c)

Die Funktion $\varphi(x)=\exp(x)$ ist konvex. Nach Jensen gilt:

$$
\exp(\mathbb E(X))
\leq
\mathbb E(\exp(X)).
$$

Damit:

$$
\mathbb E(\exp(X))
\geq
\exp\left(\frac{17}{24}\right).
$$

---

### Aufgabe 6 - 用变量变换求新随机变量的密度或分布。

#### 题目

Sie haben einen Zufallsprozess einer Standardnormalverteilung gegeben. Diesen möchten Sie nutzen, um einen fairen achtseitigen Würfel zu erhalten.

译：给定一个标准正态分布的随机过程。希望利用它构造一个公平八面骰。

###### (a)

Nutzen Sie den gegebenen Zufallsprozess, um eine stetige Gleichverteilung $U(0,1)$ zu erhalten.

译：利用给定随机过程得到一个连续均匀分布 $U(0,1)$。

###### (b)

Nutzen Sie $U$, um einen fairen stetigen Würfel auf $[1,8]$ zu erhalten. Berechnen Sie Erwartungswert und Varianz.

译：利用 $U$ 构造 $[1,8]$ 上的公平连续骰子，并计算期望和方差。

###### (c)

Nutzen Sie $U$, um einen fairen achtseitigen diskreten Würfel zu erhalten. Berechnen Sie Erwartungswert und Varianz.

译：利用 $U$ 构造一个公平八面离散骰子，并计算期望和方差。

###### (d)

Vergleichen Sie die Momente aus (b) und (c). Warum unterscheiden sie sich? Funktioniert das nur mit der Standardnormalverteilung?

译：比较 (b) 与 (c) 中的矩。它们为什么不同？这种构造是否只适用于标准正态分布？

#### 解答

##### 中文解题思路

Laplace 模型的第一步是数清样本空间大小和有利结果个数。最后概率写成 $|A|/|\Omega|$；如果事件之间有重叠，要用容斥原理而不是直接相加。

看到分布函数或密度，先检查对象类型：离散型看跳跃点概率，连续型看密度积分。分布函数要满足单调、右连续、极限从 $0$ 到 $1$；密度要非负且总积分为 $1$。

变量变换题按三步走：写出变换和反变换，求导数或 Jacobian 绝对值，再把原密度代入并写出新支持集。支持集不能省，因为它决定密度在哪些地方为零。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

正态题通常利用线性变换仍为正态：先算新变量的均值和方差，再标准化成 $Z=(X-\mu)/\sigma$ 查标准正态。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a)

Sei $Z\sim N(0,1)$ und $\Phi$ die Verteilungsfunktion der Standardnormalverteilung. Dann gilt nach der Wahrscheinlichkeitsintegraltransformation:

$$
U:=\Phi(Z)\sim U(0,1).
$$

###### (b)

Setze:

$$
Y=1+7U.
$$

Dann gilt $Y\sim U(1,8)$ und:

$$
\mathbb E(Y)=\frac{1+8}{2}=\frac92,
\qquad
\operatorname{Var}(Y)=\frac{(8-1)^2}{12}=\frac{49}{12}.
$$

###### (c)

Setze:

$$
D=\lfloor 8U\rfloor+1.
$$

Dann gilt:

$$
\mathbb P(D=i)=\frac18,
\qquad i=1,\dots,8.
$$

Daher:

$$
\mathbb E(D)=\frac92,
\qquad
\operatorname{Var}(D)=\frac{8^2-1}{12}=\frac{21}{4}.
$$

###### (d)

Die Erwartungswerte sind gleich:

$$
\mathbb E(Y)=\mathbb E(D)=\frac92.
$$

Die Varianzen unterscheiden sich:

$$
\operatorname{Var}(Y)=\frac{49}{12},
\qquad
\operatorname{Var}(D)=\frac{21}{4}.
$$

Der Grund ist, dass $Y$ stetig gleichverteilt auf dem ganzen Intervall $[1,8]$ ist, während $D$ nur die Werte $1,\dots,8$ annimmt.

Das Verfahren ist keine Spezialität der Normalverteilung. Für jede Zufallsvariable mit stetiger Verteilungsfunktion $F$ gilt $F(X)\sim U(0,1)$. Danach kann man daraus weitere Zielverteilungen konstruieren.

---

### Aufgabe 7 - 用变量变换求新随机变量的密度或分布。

#### 题目

Zeigen Sie:

译：证明：

###### (a)

Sind $X_1\sim N(\mu_1,\sigma_1^2)$ und $X_2\sim N(\mu_2,\sigma_2^2)$ unabhängig, dann ist:

译：若 $X_1\sim N(\mu_1,\sigma_1^2)$ 与 $X_2\sim N(\mu_2,\sigma_2^2)$ 独立，则：

$$
X_1+X_2\sim N(\mu_1+\mu_2,\sigma_1^2+\sigma_2^2).
$$

###### (b)

Ist $X\sim N(0,1)$, $\mu\in\mathbb R$ und $\sigma^2>0$, so gilt:

译：若 $X\sim N(0,1)$、$\mu\in\mathbb R$ 且 $\sigma^2>0$，则：

$$
Y:=\sigma X+\mu\sim N(\mu,\sigma^2).
$$

#### 解答

##### 中文解题思路

变量变换题按三步走：写出变换和反变换，求导数或 Jacobian 绝对值，再把原密度代入并写出新支持集。支持集不能省，因为它决定密度在哪些地方为零。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

正态题通常利用线性变换仍为正态：先算新变量的均值和方差，再标准化成 $Z=(X-\mu)/\sigma$ 查标准正态。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a)

Die momentenerzeugende Funktion einer Normalverteilung ist:

$$
M_X(t)=\exp\left(\mu t+\frac12\sigma^2t^2\right).
$$

Wegen Unabhängigkeit:

$$
M_{X_1+X_2}(t)=M_{X_1}(t)M_{X_2}(t).
$$

Also:

$$
M_{X_1+X_2}(t)
=
\exp\left((\mu_1+\mu_2)t+\frac12(\sigma_1^2+\sigma_2^2)t^2\right).
$$

Das ist die momentenerzeugende Funktion von $N(\mu_1+\mu_2,\sigma_1^2+\sigma_2^2)$.

###### (b)

Für $Y=\sigma X+\mu$ gilt:

$$
\mathbb E(Y)=\sigma\mathbb E(X)+\mu=\mu,
$$

und:

$$
\operatorname{Var}(Y)=\sigma^2\operatorname{Var}(X)=\sigma^2.
$$

Lineare Transformationen normalverteilter Zufallsvariablen sind wieder normalverteilt. Also:

$$
Y\sim N(\mu,\sigma^2).
$$

---

### Aufgabe 8 - 求边缘密度、条件密度或条件分布。

#### 题目

Es seien $X$ und $Y$ diskrete Zufallsvariablen mit gemeinsamer Verteilung:

译：设 $X$ 和 $Y$ 是具有如下联合分布的离散随机变量：

$$
\begin{array}{c|ccc}
X\backslash Y & -1 & 0 & 1\\
\hline
-1 & \frac3{32} & \frac5{32} & \frac1{32}\\
0 & \frac5{32} & \frac8{32} & \frac3{32}\\
1 & \frac3{32} & \frac3{32} & \frac1{32}
\end{array}
$$

###### (a)

Berechnen Sie den bedingten Erwartungswert $\mathbb E(X\mid Y)$.

译：计算条件期望 $\mathbb E(X\mid Y)$。

###### (b)

Berechnen Sie $\mathbb E(\mathbb E(X\mid Y))$.

译：计算 $\mathbb E(\mathbb E(X\mid Y))$。

#### 解答

##### 中文解题思路

条件概率题先命名事件，再把题目给出的百分比写成条件概率。若题目问的是原因在结果已知后的概率，例如 $P(K\mid T+)$，就要用 Bayes，不能直接拿敏感度或检出率当答案。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a)

Für $Y=-1$:

$$
\mathbb P(Y=-1)=\frac{11}{32},
\qquad
\mathbb E(X\mid Y=-1)=\frac{-3+3}{11}=0.
$$

Für $Y=0$:

$$
\mathbb P(Y=0)=\frac{16}{32},
\qquad
\mathbb E(X\mid Y=0)=\frac{-5+3}{16}=-\frac18.
$$

Für $Y=1$:

$$
\mathbb P(Y=1)=\frac5{32},
\qquad
\mathbb E(X\mid Y=1)=\frac{-1+1}{5}=0.
$$

Damit:

$$
\mathbb E(X\mid Y)
=
\begin{cases}
-\frac18, & Y=0,\\
0, & Y\in\{-1,1\}.
\end{cases}
$$

###### (b) Berechnen Sie $\mathbb E(\mathbb E(X\mid Y))$.

$$
\mathbb E(\mathbb E(X\mid Y))
=-\frac18\cdot\mathbb P(Y=0)
=-\frac18\cdot\frac12
=-\frac1{16}.
$$

Das stimmt mit $\mathbb E(X)$ überein:

$$
\mathbb E(X)
=-1\cdot\frac9{32}+1\cdot\frac7{32}
=-\frac1{16}.
$$

---

### Aufgabe 9 - 用卷积公式求和变量的分布。

#### 题目

Seien $X\sim U(2,5)$ und $Y\sim \operatorname{Ga}(2,2)$ stochastisch unabhängige Zufallsvariablen.

译：设 $X\sim U(2,5)$ 与 $Y\sim\operatorname{Ga}(2,2)$ 是相互独立的随机变量。

###### (a)

Sind $X-Y$ und $X+Y$ ebenfalls stochastisch unabhängig?

译：$X-Y$ 与 $X+Y$ 是否也随机独立？

###### (b)

Seien $U\sim\operatorname{Ga}(4,6)$ und $V\sim\operatorname{Exp}(\lambda)$ stochastisch unabhängig. Welchen Wert muss $\lambda$ haben, damit $U-V$ und $U+V$ unkorreliert sind?

译：设 $U\sim\operatorname{Ga}(4,6)$ 与 $V\sim\operatorname{Exp}(\lambda)$ 随机独立。$\lambda$ 应取何值，才能使 $U-V$ 与 $U+V$ 不相关？

#### 解答

##### 中文解题思路

求和分布先写卷积公式，再由支持集决定积分上下限。多数错误不在积分，而在区间分段：要同时满足 $x$ 在 $X$ 的支持上、$z-x$ 在 $Y$ 的支持上。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a) Sind $X-Y$ und $X+Y$ ebenfalls stochastisch unabhängig?

Wenn $X-Y$ und $X+Y$ unabhängig wären, dann wären sie insbesondere unkorreliert. Wir berechnen:

$$
\operatorname{Cov}(X-Y,X+Y)
=\operatorname{Var}(X)-\operatorname{Var}(Y),
$$

weil $X$ und $Y$ unabhängig sind.

Für $X\sim U(2,5)$:

$$
\operatorname{Var}(X)=\frac{(5-2)^2}{12}=\frac34.
$$

Bei der Gamma-Verteilung verwenden wir die Rate-Parametrisierung. Für $Y\sim\operatorname{Ga}(2,2)$ gilt:

$$
\operatorname{Var}(Y)=\frac{2}{2^2}=\frac12.
$$

Also:

$$
\operatorname{Cov}(X-Y,X+Y)
=\frac34-\frac12
=\frac14\neq 0.
$$

Daher sind $X-Y$ und $X+Y$ nicht unabhängig.

###### (b)

Wieder gilt:

$$
\operatorname{Cov}(U-V,U+V)
=\operatorname{Var}(U)-\operatorname{Var}(V).
$$

Mit Rate-Parametrisierung:

$$
\operatorname{Var}(U)=\frac{4}{6^2}=\frac19,
\qquad
\operatorname{Var}(V)=\frac1{\lambda^2}.
$$

Für Unkorreliertheit brauchen wir:

$$
\frac19-\frac1{\lambda^2}=0.
$$

Also:

$$
\lambda^2=9,
\qquad
\lambda=3.
$$

---

### Aufgabe 10 - 分析列联表、条件比例和关联强度。

#### 题目

Vereinfachen Sie die folgenden Ausdrücke.

译：化简下列表达式。

$$
\begin{array}{lll}
\mathbb E(3X+5)
&
\operatorname{Var}(2Y+7)
&
\operatorname{Var}\left(\frac12 Y\right)
\\[0.8em]
\operatorname{Cov}(X,Y+2Z)
&
\operatorname{Var}(X-2Y)
&
\operatorname{Cov}(X+Y,Y+3Z)
\end{array}
$$

Bemerkung: Im Allgemeinen gilt $\mathbb E(X^2)\neq \mathbb E(X)^2$.

译：备注：一般而言，$\mathbb E(X^2)\neq \mathbb E(X)^2$。

#### 解答

##### 中文解题思路

条件概率题先命名事件，再把题目给出的百分比写成条件概率。若题目问的是原因在结果已知后的概率，例如 $P(K\mid T+)$，就要用 Bayes，不能直接拿敏感度或检出率当答案。

求和分布先写卷积公式，再由支持集决定积分上下限。多数错误不在积分，而在区间分段：要同时满足 $x$ 在 $X$ 的支持上、$z-x$ 在 $Y$ 的支持上。

列联表题先补全边际总数，再看条件比例。比较两组时不要只比原始频数，因为组大小可能不同；要用条件相对频率、期望频数、$\chi^2$ 或 Odds Ratio。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

$$
\mathbb E(3X+5)=3\mathbb E(X)+5.
$$

$$
\operatorname{Var}(2Y+7)=4\operatorname{Var}(Y).
$$

$$
\operatorname{Var}\left(\frac12Y\right)=\frac14\operatorname{Var}(Y).
$$

$$
\operatorname{Cov}(X,Y+2Z)
=\operatorname{Cov}(X,Y)+2\operatorname{Cov}(X,Z).
$$

$$
\operatorname{Var}(X-2Y)
=\operatorname{Var}(X)+4\operatorname{Var}(Y)-4\operatorname{Cov}(X,Y).
$$

$$
\operatorname{Cov}(X+Y,Y+3Z)
=
\operatorname{Cov}(X,Y)
+3\operatorname{Cov}(X,Z)
+\operatorname{Var}(Y)
+3\operatorname{Cov}(Y,Z).
$$

Wichtig:

$$
\mathbb E(X^2)\neq \mathbb E(X)^2
$$

im Allgemeinen.

---

### Aufgabe 11 - 计算期望、方差、协方差或相关系数。

#### 题目

Sei:

译：设：

$$
Z\sim N(\mu_Z,\sigma_Z^2)
$$

und:

译：并且：

$$
X\mid Z=z\sim N(a+bz,\sigma^2).
$$

###### (a)

Berechnen Sie $\mathbb E(X)$.

译：计算 $\mathbb E(X)$。

###### (b)

Berechnen Sie $\operatorname{Var}(X)$.

译：计算 $\operatorname{Var}(X)$。

###### (c) Bonus

译：附加题

Sei zusätzlich $Y\sim\operatorname{Bin}(n,\pi)$ und:

译：另外设 $Y\sim\operatorname{Bin}(n,\pi)$，且：

$$
Z\mid Y=y\sim N(y,\sigma_Z^2).
$$

Berechnen Sie erneut $\mathbb E(X)$ und $\operatorname{Var}(X)$.

译：再次计算 $\mathbb E(X)$ 和 $\operatorname{Var}(X)$。

#### 解答

##### 中文解题思路

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a) Berechnen Sie $\mathbb E(X)$.

$$
\mathbb E(X)
=\mathbb E(\mathbb E(X\mid Z))
=\mathbb E(a+bZ)
=a+b\mu_Z.
$$

###### (b) Berechnen Sie $\operatorname{Var}(X)$.

Mit totaler Varianz:

$$
\operatorname{Var}(X)
=
\mathbb E(\operatorname{Var}(X\mid Z))
+
\operatorname{Var}(\mathbb E(X\mid Z)).
$$

Also:

$$
\operatorname{Var}(X)
=
\sigma^2+\operatorname{Var}(a+bZ)
=
\sigma^2+b^2\sigma_Z^2.
$$

###### (c) Bonus

Zuerst:

$$
\mathbb E(Z)=\mathbb E(\mathbb E(Z\mid Y))=\mathbb E(Y)=n\pi.
$$

Weiter:

$$
\operatorname{Var}(Z)
=
\mathbb E(\operatorname{Var}(Z\mid Y))
+
\operatorname{Var}(\mathbb E(Z\mid Y)).
$$

Damit:

$$
\operatorname{Var}(Z)
=
\sigma_Z^2+\operatorname{Var}(Y)
=
\sigma_Z^2+n\pi(1-\pi).
$$

Folglich:

$$
\mathbb E(X)=a+b n\pi,
$$

und:

$$
\operatorname{Var}(X)
=
\sigma^2+b^2\left(\sigma_Z^2+n\pi(1-\pi)\right).
$$

---

### Aufgabe 12 - 判断结果空间、事件系统和 sigma 代数性质。

#### 题目

###### (a)

Gegeben seien:

译：给定：

$$
\operatorname{Var}(X)=1,
\qquad
\operatorname{Var}(Y)=4,
\qquad
\operatorname{Var}(3X+2Y)=13.
$$

Bestimmen Sie $\rho(X,Y)$.

译：求 $\rho(X,Y)$。

###### (b)

$X$ ist gleichverteilt auf $\{1,\dots,n\}$. $Y$ nimmt die Werte $1,2,3$ an mit:

译：$X$ 在 $\{1,\dots,n\}$ 上均匀分布。$Y$ 取值 $1,2,3$，并满足：

$$
\mathbb P(Y=1)=2\mathbb P(Y=2)=4\mathbb P(Y=3),
$$

und $X,Y$ sind unabhängig.

译：并且 $X,Y$ 相互独立。

#### 解答

##### 中文解题思路

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a) Gegeben seien:

$$
\operatorname{Var}(3X+2Y)
=9\operatorname{Var}(X)+4\operatorname{Var}(Y)+12\operatorname{Cov}(X,Y).
$$

Also:

$$
13=9+16+12\operatorname{Cov}(X,Y).
$$

Damit:

$$
\operatorname{Cov}(X,Y)=-1.
$$

Da $\sigma_X=1$ und $\sigma_Y=2$:

$$
\rho(X,Y)=\frac{-1}{1\cdot2}=-\frac12.
$$

###### (b)

Setze $\mathbb P(Y=3)=t$. Dann:

$$
\mathbb P(Y=2)=2t,
\qquad
\mathbb P(Y=1)=4t.
$$

Aus $7t=1$ folgt:

$$
\mathbb P(Y=1)=\frac47,
\qquad
\mathbb P(Y=2)=\frac27,
\qquad
\mathbb P(Y=3)=\frac17.
$$

Wegen Unabhängigkeit:

$$
\mathbb P(X=i,Y=j)=\frac1n\mathbb P(Y=j),
\qquad i=1,\dots,n,\ j\in\{1,2,3\}.
$$

Außerdem:

$$
\mathbb E(X)=\frac{n+1}{2},
\qquad
\mathbb E(Y)=1\cdot\frac47+2\cdot\frac27+3\cdot\frac17=\frac{11}{7}.
$$

Daher:

$$
\mathbb E(XY)=\mathbb E(X)\mathbb E(Y)
=\frac{11(n+1)}{14}.
$$

---

### Aufgabe 13 - 计算期望、方差、协方差或相关系数。

#### 题目

Sei $X$ eine reellwertige Zufallsvariable mit endlichem zweitem Moment:

译：设 $X$ 是具有有限二阶矩的实值随机变量：

$$
E[X^2]<\infty.
$$

Zeigen Sie: $X$ hat endlichen Erwartungswert und endliche Varianz.

译：证明：$X$ 有有限期望和有限方差。

#### 解答

##### 中文解题思路

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

Aus der Cauchy-Schwarz-Ungleichung folgt:

$$
E[|X|]
=
E[|X|\cdot 1]
\leq
\sqrt{E[X^2]}\sqrt{E[1^2]}.
$$

Da $E[1^2]=1$, gilt:

$$
E[|X|]\leq\sqrt{E[X^2]}<\infty.
$$

Also existiert $E[X]$ und ist endlich.

Die Varianz ist:

$$
\operatorname{Var}(X)=E[X^2]-E[X]^2.
$$

Da beide Terme endlich sind, ist auch:

$$
\operatorname{Var}(X)<\infty.
$$

---

### Aufgabe 14 - 计算期望、方差、协方差或相关系数。

#### 题目

Sei eine Zufallsvariable

译：设随机变量：

$$
X\sim\operatorname{Exp}(\lambda),
\qquad
\lambda>0
$$

gegeben.

译：给定。

###### (a)

Zeigen Sie:

译：证明：

$$
E(X)=\frac1\lambda.
$$

###### (b)

Bestimmen Sie die Varianz von $X$.

译：求 $X$ 的方差。

#### 解答

##### 中文解题思路

看到分布函数或密度，先检查对象类型：离散型看跳跃点概率，连续型看密度积分。分布函数要满足单调、右连续、极限从 $0$ 到 $1$；密度要非负且总积分为 $1$。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a) Zeigen Sie:

Die Dichte ist:

$$
f_X(x)=\lambda e^{-\lambda x}I_{[0,\infty)}(x).
$$

Damit:

$$
E(X)=\int_0^\infty x\lambda e^{-\lambda x}\,dx.
$$

Partielle Integration mit $u=x$ und $dv=\lambda e^{-\lambda x}dx$ liefert $v=-e^{-\lambda x}$:

$$
E(X)
=
\left[-xe^{-\lambda x}\right]_0^\infty
+\int_0^\infty e^{-\lambda x}\,dx.
$$

Der Randterm ist $0$, also:

$$
E(X)=\frac1\lambda.
$$

###### (b) Bestimmen Sie die Varianz von $X$.

Zunächst:

$$
E(X^2)=\int_0^\infty x^2\lambda e^{-\lambda x}\,dx.
$$

Zweimal partielle Integration liefert:

$$
E(X^2)=\frac{2}{\lambda^2}.
$$

Damit:

$$
\operatorname{Var}(X)
=
E(X^2)-E(X)^2
=
\frac{2}{\lambda^2}-\frac{1}{\lambda^2}
=
\frac{1}{\lambda^2}.
$$

---

### Aufgabe 15 - 练习期望、方差和概率不等式。

#### 题目

Sei $X$ eine reellwertige Zufallsvariable mit

译：设 $X$ 是实值随机变量，并满足：

$$
\operatorname{Var}(X)=0.
$$

Zeigen Sie, dass ein $c\in\mathbb R$ existiert mit

译：证明存在 $c\in\mathbb R$，使得：

$$
\mathbb P(|X-c|>\varepsilon)=0
$$

für alle $\varepsilon>0$.

译：对所有 $\varepsilon>0$ 成立。

#### 解答

##### 中文解题思路

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

Setze:

$$
c=E(X).
$$

Dann:

$$
\operatorname{Var}(X)=E[(X-c)^2]=0.
$$

Nach Markovs Ungleichung gilt für $\varepsilon>0$:

$$
\mathbb P(|X-c|>\varepsilon)
=
\mathbb P((X-c)^2>\varepsilon^2)
\leq
\frac{E[(X-c)^2]}{\varepsilon^2}
=
0.
$$

Also:

$$
\mathbb P(|X-c|>\varepsilon)=0.
$$

Damit ist $X$ fast sicher konstant gleich $c$.

---

### Aufgabe 16 - 用中心极限定理或近似分布计算概率。

#### 题目

Der Skisportverband eines Landes geht davon aus, dass $1\%$ seiner Athleten unerlaubte leistungssteigernde Substanzen einnehmen. Im letzten Jahr mussten sich insgesamt $1000$ Sportler je einem Dopingtest unterziehen.

译：某国滑雪协会认为，其运动员中有 $1\%$ 使用违禁兴奋剂。去年共有 $1000$ 名运动员各接受了一次兴奋剂检测。

Sei:

译：设：

$$
X=\text{Anzahl positiver Tests}.
$$

Dann:

译：则：

$$
X\sim\operatorname{Bin}(1000,0.01).
$$

Es gilt:

译：已知：

$$
E(X)=1000\cdot0.01=10
$$

und:

译：并且：

$$
\operatorname{Var}(X)=1000\cdot0.01\cdot0.99=9.9.
$$

###### (a)

Berechnen Sie mit der Markov-Ungleichung eine obere Schranke für die Wahrscheinlichkeit, dass mehr als $15$ Dopingtests positiv ausfallen.

译：用 Markov 不等式计算超过 $15$ 个兴奋剂检测呈阳性的概率上界。

###### (b)

Schätzen Sie die Wahrscheinlichkeit, dass mehr als $5$, aber weniger als $15$ Tests positiv ausfallen, nach unten ab.

译：给出超过 $5$ 个但少于 $15$ 个检测呈阳性的概率下界。

###### (c)

Vergleichen Sie die Schranken aus (a) und (b) mit den wahren Werten.

译：将 (a) 和 (b) 的界与真实值比较。

#### 解答

##### 中文解题思路

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a)

Mit Markov:

$$
\mathbb P(X>15)
\leq
\frac{E(X)}{15}
=
\frac{10}{15}
=
\frac23.
$$

Da $X$ ganzzahlig ist, kann man auch schreiben:

$$
\mathbb P(X>15)=\mathbb P(X\geq16)
\leq
\frac{10}{16}
=
0.625.
$$

###### (b)

Das Ereignis

$$
5<X<15
$$

entspricht für ganzzahliges $X$ ungefähr dem Bereich um den Erwartungswert $10$:

$$
|X-10|<5.
$$

Mit Tschebyscheff:

$$
\mathbb P(|X-10|\geq5)
\leq
\frac{\operatorname{Var}(X)}{5^2}
=
\frac{9.9}{25}.
$$

Also:

$$
\mathbb P(5<X<15)
\geq
1-\frac{9.9}{25}
=
0.604.
$$

###### (c)

Die exakten Werte unter $X\sim\operatorname{Bin}(1000,0.01)$ sind ungefähr:

$$
\mathbb P(X>15)\approx0.0479.
$$

und:

$$
\mathbb P(5<X<15)\approx0.8514.
$$

Die Markov-Schranke aus (a) ist also sehr grob. Die Tschebyscheff-Schranke aus (b) ist ebenfalls konservativ, aber qualitativ sinnvoll.

In R:

```r
1 - pbinom(15, 1000, 0.01)
pbinom(14, 1000, 0.01) - pbinom(5, 1000, 0.01)
```

---

### Aufgabe 17 - 用 Jensen 不等式推导期望或均值不等式。

#### 题目

Es seien $a_1,\dots,a_n$ positive reelle Zahlen. Zeigen Sie mit Hilfe der Jensen-Ungleichung:

译：设 $a_1,\dots,a_n$ 是正实数。用 Jensen 不等式证明：

$$
a_H\leq a_G\leq a_A,
$$

wobei

译：其中：

$$
a_A=\frac1n\sum_{i=1}^n a_i
$$

das arithmetische Mittel,

译：表示算术平均数，

$$
a_G=\left(\prod_{i=1}^n a_i\right)^{1/n}
$$

das geometrische Mittel und

译：表示几何平均数，以及

$$
a_H=
\left(
\frac1n\sum_{i=1}^n\frac1{a_i}
\right)^{-1}
$$

das harmonische Mittel bezeichnet.

译：表示调和平均数。

#### 解答

##### 中文解题思路

Jensen 题最重要的是先判断函数凸凹。凸函数给 $\varphi(\mathbb E X)\le \mathbb E\varphi(X)$；凹函数不等号反向。写解答时要明确说明使用的是凸性还是凹性。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

Jensen 题先判断函数凸还是凹。凸函数给 $\varphi(\mathbb E X)\le \mathbb E\varphi(X)$，凹函数方向相反；考试中要把凸性判断写出来。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

Sei $X$ gleichverteilt auf $\{a_1,\dots,a_n\}$.

Da $\log$ konkav ist, gilt nach Jensen:

$$
E[\log X]\leq \log(E[X]).
$$

Also:

$$
\frac1n\sum_{i=1}^n\log(a_i)
\leq
\log\left(\frac1n\sum_{i=1}^n a_i\right).
$$

Exponentieren liefert:

$$
\left(\prod_{i=1}^n a_i\right)^{1/n}
\leq
\frac1n\sum_{i=1}^n a_i.
$$

Also:

$$
a_G\leq a_A.
$$

Für $a_H\leq a_G$ wenden wir das eben gezeigte Resultat auf die positiven Zahlen $1/a_i$ an:

$$
\left(\prod_{i=1}^n \frac1{a_i}\right)^{1/n}
\leq
\frac1n\sum_{i=1}^n\frac1{a_i}.
$$

Das ist:

$$
\frac1{a_G}\leq\frac1{a_H}.
$$

Da beide Größen positiv sind, folgt:

$$
a_H\leq a_G.
$$

Damit:

$$
a_H\leq a_G\leq a_A.
$$

---

### Aufgabe 18 - 分析分布函数、密度、众数或中位数。

#### 题目

###### (a)

Es seien $r\in[1,\infty)$, $p,q>r$ mit

译：设 $r\in[1,\infty)$，$p,q>r$，并满足：

$$
\frac1p+\frac1q=\frac1r,
$$

$X\in L^p$ und $Y\in L^q$. Beweisen Sie:

译：$X\in L^p$ 且 $Y\in L^q$。证明：

$$
\left(E(|XY|^r)\right)^{1/r}
\leq
\left(E(|X|^p)\right)^{1/p}
\left(E(|Y|^q)\right)^{1/q}.
$$

###### (b)

Es seien $X$ und $Y$ absolut stetig verteilt mit Dichten:

译：设 $X$ 和 $Y$ 绝对连续分布，密度分别为：

$$
f_X(x)=e^{-x}I_{[0,\infty)}(x),
\qquad x\in\mathbb R,
$$

und:

译：并且：

$$
f_Y(y)=\frac53y^{-8/3}I_{[1,\infty)}(y),
\qquad y\in\mathbb R.
$$

Zeigen Sie:

译：证明：

$$
E(XY)<5^{5/4}.
$$

#### 解答

##### 中文解题思路

看到分布函数或密度，先检查对象类型：离散型看跳跃点概率，连续型看密度积分。分布函数要满足单调、右连续、极限从 $0$ 到 $1$；密度要非负且总积分为 $1$。

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

题目有多个小问时，建议每个小问都保留相同的解题格式：先列已知，再写公式，再代数化简。这样即使某一问算错，也不影响其它小问的结构分。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

###### (a) Es seien $r\in[1,\infty)$, $p,q>r$ mit

Setze:

$$
\alpha=\frac{p}{r},
\qquad
\beta=\frac{q}{r}.
$$

Dann gilt $\alpha,\beta>1$ und:

$$
\frac1\alpha+\frac1\beta
=
\frac rp+\frac rq
=
r\left(\frac1p+\frac1q\right)
=
1.
$$

Mit Hölders Ungleichung:

$$
E(|XY|^r)
=
E(|X|^r|Y|^r)
\leq
\left(E(|X|^{r\alpha})\right)^{1/\alpha}
\left(E(|Y|^{r\beta})\right)^{1/\beta}.
$$

Da $r\alpha=p$ und $r\beta=q$:

$$
E(|XY|^r)
\leq
\left(E(|X|^p)\right)^{r/p}
\left(E(|Y|^q)\right)^{r/q}.
$$

Potenzieren mit $1/r$ liefert die Behauptung.

###### (b) Es seien $X$ und $Y$ absolut stetig verteilt mit Dichten:

Wir verwenden Hölder mit $r=1$, $p=4$ und $q=\frac43$:

$$
E(|XY|)
\leq
\left(E(|X|^4)\right)^{1/4}
\left(E(|Y|^{4/3})\right)^{3/4}.
$$

Für $X\sim\operatorname{Exp}(1)$ gilt:

$$
E(X^4)=4!=24.
$$

Für $Y$:

$$
E(Y^{4/3})
=
\int_1^\infty y^{4/3}\frac53y^{-8/3}\,dy
=
\frac53\int_1^\infty y^{-4/3}\,dy
=
5.
$$

Also:

$$
E(XY)
\leq
24^{1/4}\cdot 5^{3/4}.
$$

Da:

$$
24^{1/4}<5^{1/2},
$$

folgt:

$$
E(XY)<5^{1/2}\cdot5^{3/4}=5^{5/4}.
$$

---

### Aufgabe 19 - 计算期望、方差、协方差或相关系数。

#### 题目

Berechnen Sie mit Hilfe der momenterzeugenden Funktion den Erwartungswert und die Varianz einer poissonverteilten Zufallsvariable

译：利用矩母函数计算 Poisson 分布随机变量的期望和方差：

$$
Y\sim\operatorname{Poi}(\lambda),
\qquad
\lambda>0.
$$

#### 解答

##### 中文解题思路

这一类题先把已知量整理成 $\mathbb E(X)$、$\mathbb E(X^2)$、$\operatorname{Var}(X)$、$\operatorname{Cov}(X,Y)$ 等基础组件。很多题不是新技巧，而是反复使用线性性、方差缩放和协方差展开。

写最终答案时，要把关键等式链写完整：定义、代入、化简、结论四步尽量都出现。证明题尤其要避免只写直觉解释；计算题则要注明参数化方式、积分范围或条件事件。

Die momenterzeugende Funktion einer Poissonverteilung ist:

$$
M_Y(t)=E(e^{tY})=\exp(\lambda(e^t-1)).
$$

Der Erwartungswert ist:

$$
E(Y)=M_Y'(0).
$$

Ableiten:

$$
M_Y'(t)=\lambda e^t\exp(\lambda(e^t-1)).
$$

Also:

$$
E(Y)=M_Y'(0)=\lambda.
$$

Für das zweite Moment:

$$
M_Y''(t)
=
\lambda e^t\exp(\lambda(e^t-1))
+\lambda^2e^{2t}\exp(\lambda(e^t-1)).
$$

Damit:

$$
E(Y^2)=M_Y''(0)=\lambda+\lambda^2.
$$

Also:

$$
\operatorname{Var}(Y)
=
E(Y^2)-E(Y)^2
=
\lambda+\lambda^2-\lambda^2
=
\lambda.
$$

---

## 相关考试真题

### 真题 1（2021） - Aufgabe 3

#### 题目

Vier Studierende beschließen, ihr Erspartes für ein Jahr anzulegen. Dabei sei $G_i$, für $i \in \{A,B,C,D\}$, die Zufallsvariable, die den Gewinn der Investition des Studierenden $i$ beschreibt.

译：四名学生决定把积蓄投资一年。设 $G_i$（$i\in\{A,B,C,D\}$）为学生 $i$ 的投资收益随机变量。

Student A investiert $1000$ Euro in eine Kryptowährung. Der erwartete Gewinn für diese Investition nach einem Jahr betrage

译：学生 A 将 $1000$ 欧元投资于加密货币。该投资一年后的期望收益为：

$$
E(G_A)=100
$$

Euro bei einer Varianz von

译：欧元，方差为：

$$
\operatorname{Var}(G_A)=(100 \text{ Euro})^2.
$$

Studentin B steckt $1000$ Euro in Aktien aus dem DAX. Für den Gewinn aus dieser Investition nach einem Jahr gelte

译：学生 B 将 $1000$ 欧元投资于 DAX 股票。一年后该投资收益满足：

$$
E(G_B)=80
$$

Euro bei einer Varianz von

译：欧元，方差为：

$$
\operatorname{Var}(G_B)=(20 \text{ Euro})^2.
$$

Studentin C legt ihre $1000$ Euro auf einem Festgeldkonto an, sie erhält nach einem Jahr garantiert $50$ Euro Zinsen, also Gewinn.

译：学生 C 将 $1000$ 欧元存入定期账户，一年后保证获得 $50$ 欧元利息，即收益。

Gehen Sie davon aus, dass $G_A$ und $G_B$ jeweils normalverteilt sind. Außerdem sei bekannt:

译：假设 $G_A$ 和 $G_B$ 分别服从正态分布。另外已知：

$$
\operatorname{Var}(G_A+G_B)=(110 \text{ Euro})^2.
$$

##### (a)

Berechnen Sie

译：计算：

$$
\operatorname{Cov}(G_A,G_B).
$$

$(3P)$

译：（3 分）

##### (b)

Berechnen Sie den Korrelationskoeffizienten

译：计算相关系数：

$$
\rho(G_A,G_B)
$$

und interpretieren Sie diesen.  
$(3P)$

译：并解释该相关系数。（3 分）

##### (c)

Wie groß ist die Wahrscheinlichkeit, dass Student A nach einem Jahr einen höheren Gewinn erzielt hat als Studentin C?  
$(3P)$

译：学生 A 一年后收益高于学生 C 的概率是多少？（3 分）

##### (d)

Studentin D möchte ihre $1550$ Euro zu einem Teil $\alpha \in [0,1]$ auf einem Festgeldkonto mit einer garantierten Verzinsung von $6\%$ pro Jahr anlegen. Den Rest möchte sie in amerikanische Aktien mit einem erwarteten Gewinn von $10\%$ bei einer Standardabweichung von $20\%$ pro Jahr anlegen. Die Gewinne der beiden Investitionen können als unabhängig voneinander angenommen werden.

译：学生 D 想把 $1550$ 欧元中的比例 $\alpha\in[0,1]$ 投入年保证收益率为 $6\%$ 的定期账户，其余投入美国股票；股票年期望收益率为 $10\%$，标准差为 $20\%$。可假设两项投资收益相互独立。

###### (i)

Sei $\alpha=0{,}5$. Berechnen Sie den erwarteten Gesamtgewinn in Prozent der Investition für die Investition der Studentin D nach einem Jahr.  
$(3P)$

译：设 $\alpha=0{,}5$。计算学生 D 一年后总期望收益占投资额的百分比。（3 分）

###### (ii)

Welchen Anteil sollte Studentin D höchstens auf das Festgeldkonto legen, wenn der zu erwartende Gesamtgewinn mindestens $9\%$ betragen soll?  
$(3P)$

译：若期望总收益至少应为 $9\%$，学生 D 最多应将多大比例投入定期账户？（3 分）

###### (iii)

Welchen Anteil sollte Studentin D mindestens auf das Festgeldkonto legen, wenn die Standardabweichung des Gewinns nicht über $10\%$ liegen soll?  
$(4P)$

译：若收益标准差不应超过 $10\%$，学生 D 至少应将多大比例投入定期账户？（4 分）

---

#### 解答

###### (a)

###### 中文解题思路

这一步围绕矩和不等式：先写出期望、方差或要比较的函数，再决定用积分、线性性、Jensen、Markov/Chebyshev 还是正态标准化。关键是不要跳过随机变量的取值范围。

Es gilt:

$$
\operatorname{Var}(G_A+G_B)
=
\operatorname{Var}(G_A)+\operatorname{Var}(G_B)+2\operatorname{Cov}(G_A,G_B).
$$

Einsetzen liefert:

$$
110^2=100^2+20^2+2\operatorname{Cov}(G_A,G_B).
$$

Also:

$$
12100=10000+400+2\operatorname{Cov}(G_A,G_B).
$$

Daher:

$$
2\operatorname{Cov}(G_A,G_B)=1700
$$

und somit:

$$
\operatorname{Cov}(G_A,G_B)=850.
$$

###### (b)

###### 中文解题思路

这是检验两个连续变量是否线性相关。先选 Pearson-Korrelationstest，并写出 $H_0:\rho=0$ 与 $H_1:\rho\neq0$；额外假设通常是二维正态或至少相关检验条件近似成立。

Der Korrelationskoeffizient ist:

$$
\rho(G_A,G_B)
=
\frac{\operatorname{Cov}(G_A,G_B)}
{\sigma_A\sigma_B}
=
\frac{850}{100\cdot20}
=
0.425.
$$

Die Korrelation ist positiv und mittelstark. Hohe Gewinne bei A gehen also tendenziell mit höheren Gewinnen bei B einher, aber der Zusammenhang ist nicht perfekt.

###### (c)

###### 中文解题思路

这一步围绕矩和不等式：先写出期望、方差或要比较的函数，再决定用积分、线性性、Jensen、Markov/Chebyshev 还是正态标准化。关键是不要跳过随机变量的取值范围。

Studentin C erzielt sicher $50$ Euro Gewinn. Gesucht ist:

$$
\mathbb P(G_A>50).
$$

Da:

$$
G_A\sim N(100,100^2),
$$

standardisieren wir:

$$
\mathbb P(G_A>50)
=
\mathbb P\left(
\frac{G_A-100}{100}>
\frac{50-100}{100}
\right)
=
\mathbb P(Z>-0.5).
$$

Also:

$$
\mathbb P(G_A>50)=\Phi(0.5)\approx0.6915.
$$

###### (d)

###### 中文解题思路

这一步围绕矩和不等式：先写出期望、方差或要比较的函数，再决定用积分、线性性、Jensen、Markov/Chebyshev 还是正态标准化。关键是不要跳过随机变量的取值范围。

###### (i)

Bei $\alpha=0.5$ liegt die Hälfte auf dem Festgeldkonto und die Hälfte in Aktien. Der erwartete prozentuale Gewinn ist:

$$
0.5\cdot6\%+0.5\cdot10\%=8\%.
$$

Der erwartete Gesamtgewinn in Prozent beträgt also $8\%$.

###### (ii)

Der erwartete prozentuale Gewinn als Funktion von $\alpha$ ist:

$$
g(\alpha)=6\%\alpha+10\%(1-\alpha)=10\%-4\%\alpha.
$$

Gefordert ist:

$$
10-4\alpha\ge9.
$$

Damit:

$$
\alpha\le0.25.
$$

Studentin D sollte also höchstens $25\%$ auf das Festgeldkonto legen.

###### (iii)

Nur der Aktienanteil ist risikobehaftet. Die Standardabweichung des prozentualen Gewinns ist:

$$
(1-\alpha)\cdot20\%.
$$

Gefordert ist:

$$
(1-\alpha)\cdot20\%\le10\%.
$$

Also:

$$
1-\alpha\le0.5
$$

und daher:

$$
\alpha\ge0.5.
$$

Studentin D sollte also mindestens $50\%$ auf das Festgeldkonto legen.

---

### 真题 2（2021） - Aufgabe 5

#### 题目

Ein Student möchte der Vermutung nachgehen, dass man beim Radfahren tagsüber schneller auf Gefahren reagiert als nachts. Dazu misst er die Reaktionszeit in Sekunden von fünf weiteren Studierenden, $i \in \{1,\dots,5\}$, auf eine Ampel, die von grün auf rot umspringt, und zwar nachts. Die Zufallsvariable zur Reaktionszeit nachts sei $X$ und tagsüber sei $Y$:

译：一名学生想检验骑车时白天对危险的反应是否比夜晚更快。为此，他测量另外五名学生 $i\in\{1,\dots,5\}$ 在夜晚对红绿灯由绿变红的反应时间（秒）。夜晚反应时间随机变量记为 $X$，白天记为 $Y$：

| Studierende | $1$ | $2$ | $3$ | $4$ | $5$ |
|---|---:|---:|---:|---:|---:|
| $X_i$ | $1{,}9$ | $2{,}8$ | $2{,}1$ | $2{,}7$ | $2{,}3$ |
| $Y_i$ | $2{,}0$ | $2{,}5$ | $1{,}5$ | $2{,}2$ | $1{,}8$ |

##### (a)

Lässt sich die Vermutung anhand der vorliegenden Daten zu einem Signifikanzniveau von $\alpha=0{,}05$ bestätigen? Benutzen Sie dazu einen verteilungsfreien Test und geben Sie die dazugehörigen Hypothesen an. Berechnen Sie die Teststatistik analytisch, ohne R. Sie dürfen jedoch für die Berechnung der Varianz, der Standardabweichung, des Mittelwertes und der Quantile R verwenden. Treffen Sie anhand dieser die Testentscheidung.  
$(6P)$

译：基于给定数据，在显著性水平 $\alpha=0{,}05$ 下能否支持该猜想？请使用一个非参数检验，写出相应假设；不用 R，解析计算检验统计量。方差、标准差、均值和分位数可用 R 计算。并据此作出检验决策。（6 分）

##### (b)

Der Student möchte im Folgenden einen parametrischen Test verwenden.

译：接下来该学生希望使用参数检验。

###### (i)

Geben Sie einen geeigneten parametrischen Test und die zugehörigen Hypothesen an, den der Student verwenden kann. Gehen Sie dabei auch auf die benötigten Annahmen ein.  
$(4P)$

译：给出一个合适的参数检验及相应假设，并说明所需假设条件。（4 分）

###### (ii)

Gehen Sie im Folgenden davon aus, dass die Annahmen aus Teilaufgabe (b)(i) erfüllt sind. Führen Sie den Test aus Teilaufgabe (b)(i) analytisch, ohne R, durch. Sie dürfen jedoch für die Berechnung der Varianz, der Standardabweichung, des Mittelwertes und der Quantile R verwenden. Berechnen Sie dafür die Teststatistik und treffen Sie anhand dieser die Testentscheidung.  
$(4P)$

译：以下假设 (b)(i) 中的检验前提成立。请不用 R 解析执行 (b)(i) 中的检验；方差、标准差、均值和分位数可用 R 计算。计算检验统计量并作出检验决策。（4 分）

##### (c)

Eine weitere Studentin untersucht, ob der Konsum eines Energy Drinks die nächtliche Reaktionszeit beeinflusst. Dazu misst sie die nächtliche Reaktionszeit nach dem Konsum von drei Dosen des Energy Drinks. Die zugehörige Zufallsvariable sei $Z$, bei fünf weiteren zufällig und unabhängig von (a) ausgewählten Studierenden, $i \in \{6,\dots,10\}$.

译：另一名学生研究饮用能量饮料是否影响夜间反应时间。她测量另外五名随机且独立于 (a) 选出的学生 $i\in\{6,\dots,10\}$ 在喝下三罐能量饮料后的夜间反应时间，相应随机变量记为 $Z$。

| Studierende | $6$ | $7$ | $8$ | $9$ | $10$ |
|---|---:|---:|---:|---:|---:|
| $Z_i$ | $2{,}3$ | $2{,}7$ | $2{,}4$ | $2{,}2$ | $2{,}4$ |

Aus zeitlichen Gründen verwendet die Studentin die Daten ihres Kommilitonen aus (a) als Vergleichsgrundlage. Sie nimmt an, dass $X$ und $Z$ jeweils normalverteilt sind.

译：由于时间原因，该学生使用同学在 (a) 中的数据作为比较基础。她假设 $X$ 和 $Z$ 分别服从正态分布。

Welcher Test eignet sich unter diesen Bedingungen zur Überprüfung der Hypothese, dass das Getränk die mittlere nächtliche Reaktionszeit verändert? Führen Sie diesen Test durch, $\alpha=0{,}05$. Geben Sie die Hypothesen an und berechnen Sie die Teststatistik analytisch, ohne R. Sie dürfen jedoch für die Berechnung der Varianz, der Standardabweichung, des Mittelwertes und der Quantile R verwenden. Treffen Sie anhand dieser die Testentscheidung.  
$(8P)$

译：在这些条件下，适合用哪个检验来检验“该饮料改变平均夜间反应时间”的假设？在 $\alpha=0{,}05$ 下执行该检验。写出假设，不用 R 解析计算检验统计量；方差、标准差、均值和分位数可用 R 计算。并据此作出检验决策。（8 分）

---

#### 解答

###### (a)

###### 中文解题思路

这是统计检验题。先判断样本是配对还是独立、参数检验还是非参数检验；再写 $H_0$ 和 $H_1$，计算检验统计量，与临界值或 p 值比较，最后用题目语言解释是否支持原假设/研究猜想。

Die Daten sind gepaart, weil für dieselben Personen Tag- und Nachtreaktionszeiten vorliegen. Für die Vermutung „tagsüber schneller“ betrachten wir:

$$
D_i=X_i-Y_i.
$$

Große positive Werte von $D_i$ sprechen dafür, dass nachts langsamer reagiert wird.

Die Differenzen sind:

$$
(-0.1,\ 0.3,\ 0.6,\ 0.5,\ 0.5).
$$

Ein geeigneter verteilungsfreier Test ist der einseitige Wilcoxon-Vorzeichen-Rang-Test.

Die Hypothesen lauten:

$$
H_0:\operatorname{Median}(D)\le0,
\qquad
H_1:\operatorname{Median}(D)>0.
$$

Die absoluten Differenzen haben Ränge:

$$
0.1\mapsto1,\quad
0.3\mapsto2,\quad
0.5\mapsto3.5,\quad
0.5\mapsto3.5,\quad
0.6\mapsto5.
$$

Der positive Rangsummentestwert ist:

$$
W_+=2+3.5+3.5+5=14.
$$

Bei $n=5$ ist der einseitige exakte p-Wert:

$$
p=\mathbb P(W_+\ge14)=\frac{2}{32}=0.0625.
$$

Da $0.0625>0.05$, wird $H_0$ nicht verworfen. Mit dem verteilungsfreien Test lässt sich die Vermutung auf dem Niveau $5\%$ nicht bestätigen.

###### (b)

###### 中文解题思路

这是统计检验题。先判断样本是配对还是独立、参数检验还是非参数检验；再写 $H_0$ 和 $H_1$，计算检验统计量，与临界值或 p 值比较，最后用题目语言解释是否支持原假设/研究猜想。

###### (i)

Ein geeigneter parametrischer Test ist der gepaarte t-Test für die Differenzen:

$$
D_i=X_i-Y_i.
$$

Die Hypothesen sind:

$$
H_0:\mu_D\le0,
\qquad
H_1:\mu_D>0.
$$

Benötigt wird, dass die Differenzen $D_i$ annähernd normalverteilt sind bzw. dass der t-Test als Modellannahme für diese Differenzen plausibel ist.

###### (ii)

Aus den Differenzen:

$$
(-0.1,\ 0.3,\ 0.6,\ 0.5,\ 0.5)
$$

erhält man:

$$
\bar d=0.36,
\qquad
s_D\approx0.2793,
\qquad
n=5.
$$

Die Teststatistik ist:

$$
t=
\frac{\bar d-0}{s_D/\sqrt n}
\approx
\frac{0.36}{0.2793/\sqrt5}
\approx
2.88.
$$

Für $n-1=4$ Freiheitsgrade ist der kritische Wert des einseitigen Tests zum Niveau $\alpha=0.05$:

$$
t_{0.95,4}\approx2.132.
$$

Da:

$$
2.88>2.132,
$$

wird $H_0$ verworfen. Unter den parametrischen Annahmen spricht der Test dafür, dass tagsüber schneller reagiert wird.

###### (c)

###### 中文解题思路

这是统计检验题。先判断样本是配对还是独立、参数检验还是非参数检验；再写 $H_0$ 和 $H_1$，计算检验统计量，与临界值或 p 值比较，最后用题目语言解释是否支持原假设/研究猜想。

Hier werden zwei unabhängige Stichproben verglichen: die nächtlichen Reaktionszeiten ohne Energy Drink $X$ und die nächtlichen Reaktionszeiten nach Energy Drink $Z$.

Geeignet ist ein zweiseitiger Zweistichproben-t-Test. Da keine Varianzgleichheit angegeben ist, verwendet man konservativ den Welch-t-Test.

Die Hypothesen lauten:

$$
H_0:\mu_Z=\mu_X,
\qquad
H_1:\mu_Z\neq\mu_X.
$$

Die Stichprobenkennwerte sind:

$$
\bar x=2.36,
\qquad
s_X\approx0.3847,
$$

und:

$$
\bar z=2.40,
\qquad
s_Z\approx0.1871.
$$

Die Teststatistik ist:

$$
t=
\frac{\bar z-\bar x}
{\sqrt{s_X^2/5+s_Z^2/5}}
\approx
\frac{0.04}{\sqrt{0.3847^2/5+0.1871^2/5}}
\approx
0.21.
$$

Der Betrag der Teststatistik ist sehr klein. Auf dem Niveau $\alpha=0.05$ wird $H_0$ nicht verworfen.

Es gibt also keinen signifikanten Hinweis darauf, dass der Energy Drink die mittlere nächtliche Reaktionszeit verändert.

---

### 真题 3（ss2022） - Aufgabe 6 -- Stetige Dichte

#### 题目

Gegeben:

译：给定：

$$
f(x)=\frac{3}{2}(x-1)^2I_{[0,2]}(x)
$$

###### (a)

Skizze, Modus, Erwartungswert, Median, Schiefe.

译：草图、众数、期望、中位数和偏度。

###### (b)

Varianz.

译：方差。

###### (c)

Dichte von $Y=X^2$.

译：$Y=X^2$ 的密度。

###### (d)

Erwartungswert von $Z=X^4$ abschätzen.

译：估计 $Z=X^4$ 的期望。

#### 解答

Gegeben:

$$
f(x)=\frac{3}{2}(x-1)^2I_{[0,2]}(x)
$$

##### (a) Skizze, Modus, Erwartungswert, Median, Schiefe

###### 中文解题思路

先确认随机变量的密度或分布，再分别按定义处理：期望用 $\int x f(x)\,dx$，Median 解 $F(m)=0.5$，Modus 找密度最大点，Schiefe 结合均值、Median 和分布形状判断。

Die Dichte ist U-förmig und symmetrisch um $x=1$.

Modi:

$$
x=0
\qquad
\text{und}
\qquad
x=2
$$

Die Verteilung ist bimodal.

Wegen Symmetrie:

$$
E(X)=1
$$

$$
\operatorname{Median}(X)=1
$$

und:

$$
\text{Schiefe}=0
$$

##### (b) Varianz

###### 中文解题思路

这是有限测度空间上的积分计算。不要把它当普通黎曼积分；这里每个点的贡献是函数值乘以该单点的测度，最后只对积分区域里的点求和。

$$
\operatorname{Var}(X)=E(X^2)-E(X)^2
$$

Da $E(X)=1$, berechnen wir $E(X^2)$:

$$
E(X^2)
=
\int_0^2 x^2\frac{3}{2}(x-1)^2\,dx
$$

$$
=
\frac{3}{2}\int_0^2 x^2(x^2-2x+1)\,dx
$$

$$
=
\frac{3}{2}\int_0^2(x^4-2x^3+x^2)\,dx
$$

$$
=
\frac{3}{2}
\left[
\frac{x^5}{5}-\frac{x^4}{2}+\frac{x^3}{3}
\right]_0^2
$$

$$
=
\frac{3}{2}
\left(
\frac{32}{5}-8+\frac{8}{3}
\right)
$$

$$
=
\frac{3}{2}\cdot\frac{16}{15}
=
\frac{8}{5}
$$

Also:

$$
\operatorname{Var}(X)=\frac{8}{5}-1=\frac{3}{5}
$$

##### (c) Dichte von $Y=X^2$

###### 中文解题思路

变量变换题先写出新旧变量关系和取值范围。如果变换单调，用 $f_Y(y)=f_X(x(y))\lvert x'(y)\rvert$；如果不是单调，要把所有原像分支的贡献加起来。

Da $X\in[0,2]$ gilt:

$$
Y\in[0,4]
$$

Umkehrfunktion:

$$
x=\sqrt y
$$

Ableitung:

$$
\frac{dx}{dy}=\frac{1}{2\sqrt y}
$$

Dichtetransformation:

$$
f_Y(y)
=
f_X(\sqrt y)\cdot\frac{1}{2\sqrt y}
$$

$$
=
\frac{3}{2}(\sqrt y-1)^2\cdot\frac{1}{2\sqrt y}
$$

$$
=
\frac{3}{4}(\sqrt y-1)^2y^{-1/2}
$$

für $y\in[0,4]$.

Also:

$$
f_Y(y)=
\frac{3}{4}(\sqrt y-1)^2y^{-1/2}I_{[0,4]}(y)
$$

Äquivalent:

$$
f_Y(y)=
\frac{3}{4}
\left(
y^{1/2}-2+y^{-1/2}
\right)
$$

für $y\in[0,4]$.

##### (d) Erwartungswert von $Z=X^4$ abschätzen

###### 中文解题思路

先确认随机变量的密度或分布，再分别按定义处理：期望用 $\int x f(x)\,dx$，Median 解 $F(m)=0.5$，Modus 找密度最大点，Schiefe 结合均值、Median 和分布形状判断。

Mit Jensen:

$$
g(x)=x^4
$$

ist konvex.

Daher:

$$
E(X^4)\geq (E(X))^4
$$

Da $E(X)=1$ gilt:

$$
E(Z)=E(X^4)\geq 1
$$

Alternativ mit $Y=X^2$ und $g(y)=y^2$:

$$
E(X^4)\geq E(X^2)^2
$$

Da:

$$
E(X^2)=\frac{8}{5}
$$

folgt sogar:

$$
E(X^4)\geq \left(\frac{8}{5}\right)^2=\frac{64}{25}
$$

Achtung: In der OCR-Erkennung steht teils „konkav“. Korrekt ist hier **konvex**.

---

## 常见错误提醒

- **方差线性变换忘记平方**：系数提出来要平方。
  例：$Var(3X+2)=9Var(X)$。
- **算方差只算了 $E(X^2)$**：方差还要减 $E(X)^2$。
  例：$Var(X)=E(X^2)-E(X)^2$。
- **独立和不相关混用**：独立推出协方差为 0，但协方差为 0 不一定独立。
  例：只知道 $Cov(X,Y)=0$，不能直接说 $X,Y$ 独立。
- **Markov/Chebyshev 用错条件**：Markov 要非负或绝对值，Chebyshev 要均值和方差。
  例：$P(|X-\mu|\ge2)\le Var(X)/4$ 是 Chebyshev。
- **正态标准化漏除以标准差**：不是除以方差。
  例：$Z=(X-\mu)/\sigma$，不是 $(X-\mu)/\sigma^2$。
- **检验题不写假设**：没有 $H_0,H_1$，后面算对也容易丢分。
  例：配对题先写 $H_0:\mu_D=0$，再算 $t$。

## 本章必会题型清单

- 会计算期望、$E(X^2)$ 和方差。
- 会处理线性变换下的期望和方差。
- 会计算协方差和相关系数。
- 会使用 Markov、Chebyshev、Jensen。
- 会把正态变量标准化并查概率。
- 会区分配对 t、Welch t 和 Wilcoxon 检验。
- 会写假设、统计量、决策和结论。

---
